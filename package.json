{
  "name": "disapproval_api",
  "version": "1.0.0",
  "description": "Provides an API for disapproval usage",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "gh-pages -d build"
  },
  "author": "Michele Gargiulo / Stefano Giometti",
  "license": "ISC",
  "devDependencies": {
    "gh-pages": "^0.12.0"
  }
}
